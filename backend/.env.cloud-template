# ============================================================
# ZMART Backend - Cloud Supabase Configuration Template
# ============================================================
# Instructions: Replace placeholder values with your actual credentials
# from Cloud Supabase setup
#
# Get these values from:
# 1. Supabase Dashboard → Settings → API
# 2. Supabase Dashboard → Settings → Database
#
# Security: Never commit .env to git! (.gitignore already configured)
# ============================================================

# ---------------------------------
# Supabase Cloud Configuration
# ---------------------------------
# From: Supabase Dashboard → Settings → API → Project URL
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co

# From: Supabase Dashboard → Settings → API → Project API keys → anon (public)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON_KEY_HERE

# From: Supabase Dashboard → Settings → API → Project API keys → service_role (secret)
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_SERVICE_ROLE_KEY_HERE

# From: Supabase Dashboard → Settings → Database → Connection string → URI
# IMPORTANT: Replace [YOUR-PASSWORD] with your actual database password!
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.YOUR_PROJECT_ID.supabase.co:5432/postgres

# ---------------------------------
# Solana Configuration (Keep as-is)
# ---------------------------------
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet
BACKEND_KEYPAIR_PATH=/Users/seman/.config/solana/backend-authority.json

# Anchor Program IDs (Update after deployment)
ZMART_CORE_PROGRAM_ID=7h3gXfBfYFueFVLYyfL5Qo1QGsf4GQUfW96FKVgnUsJS

# ---------------------------------
# API Configuration (Keep as-is)
# ---------------------------------
API_PORT=4000
WEBSOCKET_PORT=4001
NODE_ENV=development

# ---------------------------------
# Redis Configuration (Keep as-is)
# ---------------------------------
REDIS_URL=redis://localhost:6379

# ---------------------------------
# Services Configuration (Keep as-is)
# ---------------------------------
# Vote Aggregator: Runs every 5 minutes
VOTE_AGGREGATOR_CRON="*/5 * * * *"

# IPFS Snapshot: Runs daily at midnight UTC
IPFS_SNAPSHOT_CRON="0 0 * * *"

# ---------------------------------
# Optional: IPFS Configuration
# ---------------------------------
# IPFS_HOST=127.0.0.1
# IPFS_PORT=5001
# IPFS_PROTOCOL=http

# ============================================================
# Next Steps After Filling In Credentials:
# ============================================================
# 1. Save this file as .env (remove .cloud-template)
# 2. Run: npm run test:db
# 3. Run: npm run build && npm start
# 4. Verify Vote Aggregator connects to cloud database
# ============================================================
